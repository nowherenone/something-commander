appId: com.something-commander.app
productName: Something Commander
directories:
  buildResources: build
files:
  - '!**/.vscode/*'
  - '!src/*'
  - '!e2e/*'
  - '!plugin-template/*'
  - '!electron.vite.config.{js,ts,mjs,cjs}'
  - '!{.eslintignore,.eslintrc.cjs,.prettierignore,.prettierrc.yaml,dev-app-update.yml,CHANGELOG.md,README.md}'
  - '!{.env,.env.*,.npmrc,pnpm-lock.yaml}'
  - '!{tsconfig.json,tsconfig.node.json,tsconfig.web.json}'
  - '!{vitest.config.ts,vite.test.config.ts,playwright.config.ts}'
asarUnpack:
  - resources/**

# Windows
win:
  executableName: something-commander
  signAndEditExecutable: false
  icon: build/icon.ico
  target:
    - target: nsis
      arch:
        - x64
    - target: dir
      arch:
        - x64
nsis:
  oneClick: false
  allowToChangeInstallationDirectory: true
  artifactName: Something-Commander-${version}-setup.${ext}
  shortcutName: Something Commander
  uninstallDisplayName: Something Commander
  createDesktopShortcut: always
  createStartMenuShortcut: true

# macOS
mac:
  icon: build/icon.icns
  entitlementsInherit: build/entitlements.mac.plist
  notarize: false
  target:
    - target: dmg
      arch:
        - x64
        - arm64
dmg:
  artifactName: Something-Commander-${version}.${ext}
  format: UDBZ

# Linux
linux:
  icon: build/icon.png
  target:
    - target: deb
      arch:
        - x64
    - target: AppImage
      arch:
        - x64
  maintainer: Something Commander Contributors
  category: Utility
  description: A modern orthodox two-panel file manager
deb:
  artifactName: something-commander-${version}.${ext}
appImage:
  artifactName: Something-Commander-${version}.${ext}

npmRebuild: false
